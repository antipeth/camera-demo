<div>
  <button id="toggleButton" class="px-4 py-2 bg-blue-500 text-white rounded">
    开启摄像头
  </button>

  <video
    id="videoElement"
    width="100%"
    height="auto"
    autoPlay
    playsInline
    style="display: none;" 
  />
</div>

<script>
  let hasPermission = false;
  let stream = null;

  // 获取 DOM 元素
  const videoElement = document.getElementById('videoElement');
  const toggleButton = document.getElementById('toggleButton');

  const startVideo = async () => {
    try {
      // 请求摄像头权限
      stream = await navigator.mediaDevices.getUserMedia({ video: true });
      hasPermission = true;
      videoElement.srcObject = stream;
      videoElement.style.display = 'block';  // 显示 video 元素
      toggleButton.textContent = '关闭摄像头';  // 更新按钮文本
    } catch (err) {
      console.error('Error accessing webcam:', err);
      hasPermission = false;
    }
  };

  const stopVideo = () => {
    if (stream) {
      const tracks = stream.getTracks();
      tracks.forEach(track => track.stop());
      stream = null;
      hasPermission = false;
      videoElement.srcObject = null;
      videoElement.style.display = 'none';  // 隐藏 video 元素
      toggleButton.textContent = '开启摄像头';  // 恢复按钮文本
    }
  };

  // 为按钮添加事件监听
  toggleButton.addEventListener('click', () => {
    if (hasPermission) {
      stopVideo();
    } else {
      startVideo();
    }
  });
</script>
